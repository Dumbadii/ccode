#include <stdio.h>
#define MAXLINE 80
#define MAXFILE 1000

int getlinex(char line[], int maxline);
int appendline(char fi[], char line[], int maxfile);
void removecomments(char fi[], int maxfile);

int main() {
    char fi[MAXFILE];
    char newfi[MAXFILE];
    char line[MAXLINE];


}
